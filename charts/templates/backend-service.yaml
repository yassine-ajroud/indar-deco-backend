apiVersion: v1
kind: Service
metadata:
  name: backend
spec:
  ports:
    - port: {{ .Values.backend.port }}
      targetPort: {{ .Values.backend.port }}
  selector:
    app: backend
  type: ClusterIP